{
  "dependencies": {
    "mongodb": "^7.1.0"
  },
  "name": "streamix",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": "18.x"
  }
}
